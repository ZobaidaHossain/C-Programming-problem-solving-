#include <stdio.h>
int main(void){
    int m,n,a;
    printf("enter the three number:");
    scanf("%d%d%d",&m,&n,&a);
    if(m>n && m>a){
        printf("m is the large number");}
    else if(n>a && n>m){
        printf("n is the large number");
    }
      else if("a>m && a>n") {
        printf("a is the larger number");
      }


    return 0;

}
